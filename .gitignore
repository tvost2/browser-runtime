# dependencies & toolchains — fetched by npm install / npm run setup:*
node_modules/
tools/emsdk/

# Babylon.js clone — only for `npm run analyze` / `bench:baseline`
reference/

# build outputs — `npm run build`
web/dist/
web/backend/engine.mjs
web/backend/engine.wasm
web/backend/engine.build.json
web/backend/engine-o3.mjs
web/backend/engine-o3.wasm
web/backend/engine-o3.build.json
web/backend/engine-debug.mjs
web/backend/engine-debug.wasm
web/backend/engine-debug.build.json

# native test/bench binaries — recompiled by test:equivalence / bench:native
native/build/
native/build-wasm/
*.exe
*.o
*.obj

# regenerated by `node native/tests/gen_fixtures.mjs`
native/tests/fixtures/*.bin
native/tests/fixtures/manifest.json
# GLB: hand-authored fixtures (tri/two-boxes) are vendored; real ones are fetched
native/tests/fixtures/glb/real/

# benchmark run outputs. bench/results/*.json + HARDWARE.md + engine-demo.png
# are committed as the v0.1.0 reference; everything else is regenerable.
bench/results/*
!bench/results/*.json
!bench/results/HARDWARE.md
!bench/results/engine-demo.png
bench/results/engine-demo.json
bench/results/visual.json

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# generated by `npm run analyze` (large; regenerable)
tools/migration-analyzer/report.json

# side-by-side viewer static build (96MB, deploy artifact)
web/harness/vitrine-compare/dist/
web/harness/vitrine-compare/Caddyfile.snippet
