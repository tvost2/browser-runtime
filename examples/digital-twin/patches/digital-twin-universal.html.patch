--- .bak-browser-runtime/digital-twin-universal.html	2026-09-03 16:33:17.676635772 +0000
+++ digital-twin-universal.html	2026-09-03 16:33:39.197966027 +0000
@@ -39,6 +39,18 @@
     }
     html, body { margin: 0; height: 100%; background: var(--bg); font-family: system-ui, sans-serif; color: var(--text); overflow: hidden; }
     #renderCanvas { width: 100%; height: 100%; display: block; touch-action: none; }
+    /* Browser Runtime (C++/WASM + WebGPU) overlay — opaque, stacked over
+       the Babylon canvas, shown only while the engine toggle is on. */
+    #runtimeCanvas { position: fixed; inset: 0; width: 100%; height: 100%; display: none; z-index: 2; background: #05060a; touch-action: none; pointer-events: none; }
+    #runtimeHud {
+      position: fixed; left: 12px; bottom: 12px; z-index: 6; display: none;
+      background: rgba(5,7,12,0.82); border: 1px solid #2a3a52; border-radius: 8px;
+      padding: 8px 11px; font: 11px/1.55 ui-monospace, monospace; color: #b8c7dc; white-space: pre;
+    }
+    #runtimeHud b { color: #8fd0ff; }
+    #engineToggleRow { margin: 8px 0; padding: 6px 8px; background: var(--accent-bg); border: 1px solid var(--panel-border); border-radius: 5px; }
+    #engineToggleRow label { display: flex; align-items: center; gap: 7px; cursor: pointer; font-size: 12px; }
+    #engineStatus { color: var(--text-dim); font-size: 10px; margin-top: 3px; min-height: 12px; }
     .panel { position: fixed; background: var(--panel-bg); border: 1px solid var(--panel-border); border-radius: 8px; padding: 12px 14px; font-size: 12px; line-height: 1.5; z-index: 5; }
     #topPanel { top: 12px; left: 12px; width: 320px; }
     #topPanel h1 { font-size: 14px; margin: 0 0 8px; color: var(--accent); display: inline-block; }
@@ -124,6 +136,8 @@
 </head>
 <body>
   <canvas id="renderCanvas"></canvas>
+  <canvas id="runtimeCanvas"></canvas>
+  <div id="runtimeHud"></div>
   <div id="streetLabels"></div>
   <div class="hint">Precisa ser servido via <code>node serve.mjs .</code> (não abra como file://).</div>
 
@@ -153,6 +167,10 @@
         Aplicar Sparse Geometry Engine (10%, opcional — padrão é RAW/100%, representação correta)
       </label>
     </div>
+    <div id="engineToggleRow">
+      <label><input id="engineToggle" type="checkbox" /> Motor <b>Browser Runtime</b> (C++/WASM + WebGPU)</label>
+      <div id="engineStatus">Babylon.js (WebGL) — padrão</div>
+    </div>
     <div id="streetSearchRow">
       <input id="streetSearchInput" type="text" placeholder="Buscar rua real (ex: Afonso Pena)..." autocomplete="off" />
       <div id="streetSearchResults"></div>
